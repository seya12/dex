/*
create new ERC20 token + swap function
swap function: remember address and open "change"
OR
unique swap contract with owner rights, only I can make the swaps?
*/

const Token = () => {
  return <h1>Create new Token</h1>;
};

export default Token;
